# Runtime dependencies
alpaca-trade-api>=3.1.1,<4.0
pandas>=2.0,<3.0
numpy>=1.24,<3.0
# Loads ALPACA_* variables from a local .env file at startup.
python-dotenv>=1.0,<2.0
# yfinance is the default backtest data source (free, no credentials).
# Can be removed if you only use the Alpaca data source.
yfinance>=0.2.40

# Development / testing
pytest>=7.4,<9.0
